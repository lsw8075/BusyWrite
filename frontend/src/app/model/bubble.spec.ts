import { LeafBubble, InternalBubble, SuggestBubble } from './bubble';
import { Note } from './note';

describe('LeafBubble', () => {

  it('can create new instance with Note object', () => {
    let note: Note;

    expect(1).toEqual(2);
  });
});


describe('SuggestBubble', () => {

  it('can create new instance with Note object', () => {
    let note: Note;

    expect(1).toEqual(2);
  });
});

