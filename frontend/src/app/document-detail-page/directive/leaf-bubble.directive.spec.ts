import { LeafBubbleDirective } from './leaf-bubble.directive';

describe('LeafBubbleDirective', () => {
  it('should create an instance', () => {
    const directive = new LeafBubbleDirective();
    expect(directive).toBeTruthy();
  });
});
